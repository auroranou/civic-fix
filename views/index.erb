<div class="index">
	<p>Civic Fix is a platform for citizen (and non-citizen) empowerment. Post public service announcements for your neighborhood and email local government without the hassle of bureaucracy. Sign up to start using Civic Fix today!</p>
</div>

<div class="index">
	<% @posts.each do |p| %>
		<div class="post">
			<h2><%= p.title %></h2>
			<div class="date"><%= p.created_at %> | Posted by <%= p.user.name %> from <%= p.user.zipcode %></div>
			<p><%= p.description %></p>
		</div>
	<% end %>
</div>